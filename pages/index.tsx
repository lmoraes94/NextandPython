import type { NextPage } from "next";
import CabecalhoContainer from "../src/components/Header/Header";
const Home: NextPage = () => {
  return (
    <div>
      <CabecalhoContainer />
    </div>
  );
};

export default Home;
